---
category: Foundations
---
# InfoIcon

The small outlined `i` after any figure that has an explanation. Always give it a `label` (what it explains). Electrolyte ring on totals.

```tsx
<span>Resting <InfoIcon label="resting energy" onClick={explain} /></span>
```

**Dart source (promote from):** `lib/features/macro_dashboard/presentation/widgets/breakdown_pager.dart`
