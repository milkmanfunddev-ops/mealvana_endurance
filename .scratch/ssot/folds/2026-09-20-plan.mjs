// Fold plan, 2026-09-20. Each card: decisions folded in (`from`), questions it answers (`answers`).
// Clause tags: [built] ratifies what is in the code, [to build] is new work, [no work] closes a question.
const L = a => a.map((s, i) => `${i + 1}. ${s}`).join('\n');

export const plan = [
  {
    from: ['mp-032', 'mp-033', 'mp-034', 'mp-035', 'mp-036', 'mp-038', 'mp-313', 'mp-314', 'mp-333', 'mp-347'],
    answers: ['mp-315', 'mp-334', 'mp-349', 'mp-351'],
    into: {
      title: 'Vana\'s memory: what was built after your rethink, and four loose ends',
      category: "Vana's memory", screen: 'none (algorithm/data)', work: 'pending',
      question: 'Is the memory build that followed mp-277 right, and how do the four questions it raised get answered?',
      context: 'On 09-14 you held six memory cards and asked for a rethink. On 09-15 you approved mp-277 (history in chunks, notes as they happen, one summary when a conversation goes idle). Tickets 13 to 15 built it. This card replaces the six held cards, ratifies what the tickets built, and answers the four questions the agents left.',
      decision: L([
        'The five held cards about the 20-message replay and the mid-conversation episode are retired. mp-277 replaced them. (mp-032 to mp-036) [no work]',
        'Past conversations reach Vana as a LAST TALKS line: what the athlete said, not a read-out of their schedule. Memories hold only notes. (mp-038) [built]',
        'A long conversation is summarised in chunks of twenty messages and the summary is kept on the conversation. (mp-333) [built]',
        'The conversation list previews each conversation with its one-sentence summary. The internal "Through message 20…" text never shows on screen. (mp-334) [to build]',
        'When the app says a conversation is idle, the server answers at once and writes the summary in the background. A repeat signal writes nothing. (mp-347) [built]',
        'If a conversation gains messages after its summary was written, the next close summarises it again. One summary per conversation, always the latest. (mp-349) [to build]',
        'Only a change the athlete makes through Vana, or a new day, rebuilds the cached context. Notes the server writes for itself do not. A home location, a thumb or a profile edit made outside Vana waits for the next of those. (mp-313, mp-315) [built]',
        'The context block is capped at 1,500 estimated tokens, and a test fails if it grows past that. (mp-314) [built]',
        'The "Remembered" card shows only when the athlete asked Vana to remember something. Notes she saves on her own stay silent, as mp-027 already says. (mp-351) [to build]',
      ]),
      why: 'One card instead of fourteen. Clauses 4, 6 and 9 are the only new work; the rest is already in the code and on dev.',
      alternatives: 'Ruling each of the fourteen cards separately.',
      touches: 'vana-chat history and summary, the context block, the conversation list, the Remembered card.',
      details: 'Summary boundaries are fixed multiples of twenty so the cached prefix changes once per chunk. Context budget constant CONTEXT_BLOCK_TOKEN_BUDGET = 1500, estimated as characters / 4. Idle is answered 202 and never charged a credit.',
    },
  },
  {
    from: ['mp-311', 'mp-312', 'mp-359'],
    answers: ['mp-316', 'mp-353'],
    into: {
      title: 'Prompt caching: how Vana\'s turns stay cheap',
      category: "Vana's voice and openers", screen: 'none (algorithm/data)', work: 'pending',
      question: 'How is Claude\'s prompt cache used, and should a brand-new conversation read from it too?',
      context: 'You approved mp-276: the part of the prompt that repeats every turn is cached, and the athlete context is built once per conversation. Ticket 13 built it. On dev, turn one of a conversation sent about 10,900 tokens and later turns about 6,000, most of it read from the cache. The first turn of every new conversation still reads nothing from the cache.',
      decision: L([
        'Caching is switched on for every Vana call in Anthropic\'s automatic mode, and the call log records cached tokens. (mp-311) [built]',
        'The athlete context is stored on the conversation and rebuilt only on a new day or when the athlete changes something through Vana. (mp-311) [built]',
        'Anything that changes every message (the screen the athlete is on, the entity in view) rides on the athlete\'s message, never in the system prompt. A screen gets its "in view" section by adding one row to the server\'s screen table. (mp-312, mp-359) [built]',
        'The tools and persona, which are the same for every athlete, get their own cache breakpoint, and the athlete context gets a second one. Today a planning turn reads only 43% of its input from the cache, because every plan change rebuilds the context and throws away the 12,000 tokens of tools and persona with it. (mp-316) [to build]',
        'A replayed conversation is byte-for-byte what was sent the first time: the screen line and the opener\'s hidden first message are stored with the transcript. Today they are not, so each new turn misses the cache written by the last. [to build]',
        'The call log also records cache-write tokens, the number of model steps, the gateway\'s own charge and whether the turn cost a credit, so October\'s real traffic can set the numbers. [to build]',
        'The opener\'s start-time test stays at 3.5 seconds. The way to tighten it is a faster context build, not a looser test. (mp-353) [no work]',
      ]),
      why: 'Cached input costs a tenth of fresh input. Clause 4 cuts a planning turn from about 3.2 cents to about 1.2 cents, roughly 40% of all AI cost. Clause 5 adds about 6%.',
      alternatives: 'Marking individual prompt blocks by hand instead of automatic mode. Leaving the first turn uncached.',
      touches: 'vana-chat, vana-action, jade-chat, vana-day-notes; the conversation and call-log tables.',
      details: 'Audit 2026-09-20, dev, last 30 days: cache read share 43% on planning turns, 65% on planning openers, 52% on general openers. Dev conversation 614dbee6: turn 1 input 10,886, cache read 4,980; turn 2 input 6,075, cache read 4,980; turn 3 input 6,123, cache read 6,072. Migration 20260915120000 added context, context_day and cache_read_tokens. The cost report of 2026-09-20 has the full audit.',
    },
  },
  {
    from: ['mp-363', 'mp-364'],
    answers: ['mp-352', 'mp-367', 'mp-368', 'mp-369', 'mp-377', 'mp-384', 'mp-394'],
    into: {
      title: 'Openers: what Vana says first, and where',
      category: "Vana's voice and openers", screen: 'Vana sheet', work: 'pending',
      question: 'What does Vana open on in each place, and what happens when the athlete asks for a plan?',
      context: 'mp-268 says the general conversation opens on the screen underneath. Ticket 28 built it and left seven questions about which screen, which order and which chips. On 09-16 you also ruled that Vana drafts every opener herself and never picks from templates.',
      decision: L([
        'The order is: a live moment (a fuelling window) first, then the screen underneath, then the personal opener. (mp-364, mp-368) [built]',
        'A request for a plan is a plan request even with a constraint ("quick dinners this week"). Vana hands off to the meal-planning screen and the constraint goes with it. (mp-363) [built]',
        'When the opener offers to start a meal plan, that offer is the hand-off button itself. No chip that then produces a button. (mp-377) [to build]',
        'The Plan tab with a plan in view opens on that plan. (mp-367) [to build]',
        'Ask Vana from the formula editor opens on the formula. (mp-394) [to build]',
        'The full-screen chat asks for an opener the same way the sheet does, instead of opening empty. (mp-369) [to build]',
        'Picking up last time in an offer chip counts. The opener\'s sentence does not have to say it as well. (mp-352) [no work]',
        'The launcher stays on its three screens (mp-264). mp-268\'s example of opening on an event is reached through the hand-off, not by putting the launcher on the event screen. (mp-384) [no work]',
      ]),
      why: 'Each answer follows a ruling you already made: the screen underneath (mp-268), three launcher screens (mp-264), hand-offs for every flow the app owns (mp-265).',
      alternatives: 'Widening the launcher to event, meal and session screens (clause 8). Letting the screen underneath outrank a live moment (clause 1).',
      touches: 'The opener path in vana-chat, the sheet, the full-screen chat, the Plan tab, the formula editor.',
      details: '"The screen says something useful" is decided by resolving the Situation with and without the entity id: a different sentence means the athlete\'s own row was read. The opener logs which of the three paths fired.',
    },
  },
  {
    from: ['mp-385', 'mp-386'],
    answers: ['mp-370', 'mp-371', 'mp-393', 'mp-396', 'mp-403'],
    into: {
      title: 'What Vana is told about the screen in view',
      category: 'Situation awareness', screen: 'Formula editor, Plan tab', work: 'pending',
      question: 'What does each screen tell Vana, and why did her carb total differ from the formula editor\'s?',
      context: 'mp-273 gives every entry point the same athlete context plus one short section for what is on screen. mp-274 lets the formula editor send its unsaved draft. Tickets 16 and 17 built both. On the simulator Vana said 142 g of carbs for a draft the editor showed as 133 g.',
      decision: L([
        'The formula editor sends its draft as fixed fields only: phase, sub-phase, durations, activities, component ids with quantities, and a name of at most 40 characters. A draft from any other screen is dropped without an error. (mp-385, mp-396) [built]',
        'The server looks the component names up from the athlete\'s own foods. The app sends ids, never names. (mp-385) [built]',
        'The formula section carries the editor\'s own carb total. Vana repeats that number and never adds it up herself, so the two can never disagree. (mp-386, mp-393) [to build]',
        'A draft that has a scope (a sub-phase, a duration or an activity) is described by that scope, not called empty. (mp-403) [to build]',
        'The Plan tab\'s section speaks for the day on screen, plus one line on the week\'s servings left. (mp-371) [to build]',
        'The persona names the in-view section in one sentence, the way it names the Situation. (mp-370) [to build]',
      ]),
      why: 'Clause 3 is a bug an athlete would see. Fuelling numbers never come from the model (mp-005), so the fix is to hand her the app\'s number.',
      alternatives: 'Letting Vana compute totals from the quantities. Sending the whole week in the Plan tab section.',
      touches: 'The Situation resolver, the formula editor, the Plan tab section, the persona.',
    },
  },
  {
    from: ['mp-326', 'mp-348', 'mp-360', 'mp-361', 'mp-362', 'mp-387'],
    answers: ['mp-350', 'mp-372', 'mp-373', 'mp-374', 'mp-375', 'mp-376'],
    into: {
      title: 'The sheet, the launcher and the hand-off button',
      category: 'The sheet and launcher', screen: 'Vana sheet', work: 'pending',
      image: 'docs/ssot/decisions/images/mealplanning/timeline-launcher.png',
      question: 'How do the three-screen launcher, the one-height sheet and the hand-off button work in practice?',
      context: 'You approved mp-264 (Vana on three screens), mp-265 (one sheet height, every flow the app owns is a hand-off) and mp-275 (what continues the day\'s conversation). Tickets 15, 16, 17, 23 and 27 built them. This card ratifies the build and answers six questions from it.',
      decision: L([
        'The launcher shows on the main tabs, the meal-planning screen and the formula library, and hides whenever anything is pushed or popped over them. No screen has to name itself. (mp-326) [built]',
        'The sheet is three quarters of the screen. It closes by the platform\'s own rule, a drag past half or a flick. The grabber is only a handle. (mp-361) [built]',
        'A hand-off is a button Vana offers, labelled in the athlete\'s own words (60 characters at most), that opens the screen that owns the job: meal plan, new activity, event. It works in the full-screen chat too. (mp-362) [built]',
        'Vana offers a hand-off only when she can name the thing. With no event named she asks which one, instead of landing on a blank form or a bare list. (mp-374) [to build]',
        'A carb-loading hand-off opens the carb-loading picks for that event directly. (mp-373) [to build]',
        'A fuelling hand-off edits the workout Vana named and never creates a second one. This gets checked on the simulator. (mp-375) [to build]',
        'One controller decides which conversation is "today\'s". New meal plan, the plus button and Ask Vana in the formula editor each start a new conversation and never move that pointer. (mp-360, mp-387, mp-372) [built]',
        'The app tells the server a conversation is idle when the sheet closes, the app goes to the background, a new conversation starts, or the athlete leaves the full-screen chat. At most once per opening. (mp-348, mp-350) [built, except leaving the full-screen chat]',
        'Closed as already done: two new conversations sharing one screen was fixed on 09-16 (761d4d6f), and the status chip question is gone because you had the chip deleted on 09-16. (mp-372, mp-376) [no work]',
      ]),
      why: 'Clauses 1 to 3 and 7 are in the code. Clauses 4 to 6 finish the hand-off so it never lands somewhere useless.',
      alternatives: 'Landing on a blank form when no entity is named. Keeping carb loading on the event screen.',
      touches: 'The launcher rule, the sheet, the hand-off part, the ambient conversation controller, the carb-loading picks, the new-activity screen.',
      details: 'Launcher rule test walks every route the router declares. Hand-off targets: meal plan -> meal-planning page, fuelling -> new-activity screen, event and carb loading -> event screen (today).',
    },
  },
  {
    from: ['mp-365', 'mp-366', 'mp-358'],
    answers: ['mp-378', 'mp-379', 'mp-380', 'mp-381', 'mp-382'],
    into: {
      title: 'The planning period: start day and length',
      category: 'Plan tab', screen: 'Vana settings', work: 'pending',
      question: 'How do a start day and a period other than seven days behave?',
      context: 'You approved mp-269: the week\'s start day and the period\'s length are the athlete\'s settings. Ticket 29 built them. Seven days from Sunday was the only shape the code knew, so a ten-day period raised five questions.',
      decision: L([
        'The two settings sit in Vana settings: a popup of the seven days and a stepper from 3 to 14 days. Cooking labels use the athlete\'s own day ("Cook Monday"). (mp-366, mp-365) [built]',
        'Every place that works out a week or a cook day reads the athlete\'s period, never the Sunday default. (mp-358) [built]',
        'Periods follow each other with no overlap. A ten-day period is followed by the next ten-day period, starting where the last one ended. (mp-379) [to build]',
        'A plan records the period it was built for, so changing the setting never re-reads an old plan. (mp-380) [to build]',
        'Changing the start day mid-period leaves the current plan alone until it ends. The new setting applies from the next period. (mp-378) [to build]',
        'The reminder text names the athlete\'s own days. (mp-381) [to build]',
        'Vana can change both settings in conversation ("my week starts Monday"). (mp-382) [to build]',
      ]),
      why: 'Clauses 3 to 5 are one fix: once a plan carries its own period, overlap and mid-period changes stop being problems.',
      alternatives: 'Keeping a weekly cadence with overlapping periods. Moving the current plan when the start day changes.',
      touches: 'Vana settings, the Plan tab, the plan row (one new column), coverage, reminders, Vana\'s settings tool.',
      details: 'Length limited to 3 to 14 days: below three the three cooking sessions collapse onto one day. The period length travels on the coverage wire and the week part.',
    },
  },
  {
    from: ['mp-389', 'mp-390', 'mp-391', 'mp-392'],
    answers: ['mp-397', 'mp-398', 'mp-399', 'mp-400', 'mp-401', 'mp-402', 'mp-404'],
    into: {
      title: 'Batches, servings and coverage',
      category: 'Plan tab', screen: 'Plan tab', work: 'pending',
      question: 'How big is a batch, what does coverage count, and where is "same as last time"?',
      context: 'You approved mp-231: a plan fills a cooking period, not fourteen fixed slots. Ticket 30 built it and left seven questions.',
      decision: L([
        'A batch is three meals per meal type. Servings are the period divided by three, rounded up: 7 days gives 3, 10 gives 4, 14 gives 5. A per-day planner gets one serving per pick. (mp-389) [built]',
        'Coverage counts the period\'s days times the meal types the athlete plans. Someone who never chose keeps lunch and dinner, so existing plans\' numbers do not move. (mp-390) [built]',
        'Choosing "every meal" counts four slots a day. (mp-398) [to build]',
        'A per-day planner cooking two servings covers two nights, not one. (mp-399) [to build]',
        '"Same as last time" copies last period\'s meals at the servings they were cooked at and never doubles a meal already in the draft. (mp-391) [built]',
        'Its tap is a button on the Plan tab\'s empty week, and Vana offers it in the planning opener. (mp-400) [to build]',
        'Suggestions rank liked and already-cooked meals first, as mp-231 says. This gets its own ticket. (mp-401) [to build]',
        'Vana settings gets a meal-type picker, so the athlete can choose what they plan without asking Vana. (mp-397) [to build]',
        'A plan with no cooking mode on it means batch, in the app and on the server. (mp-392) [built]',
        'Where coverage is unknown the staples check says nothing, instead of assuming fourteen slots. The shared test fixture carries the inputs so both sides work the answer out themselves. (mp-402, mp-404) [to build]',
      ]),
      why: 'Clauses 1, 2, 5 and 9 are built. The rest finish mp-231 where ticket 30 stopped.',
      alternatives: 'Rescaling copied meals to the new period (invented numbers). Defaulting every pick to four servings.',
      touches: 'The Plan tab, coverage on both sides, the planning conversation, Vana settings, suggestion ranking.',
    },
  },
  {
    from: ['mp-406', 'mp-407', 'mp-408', 'mp-409'],
    answers: ['mp-410', 'mp-411', 'mp-412'],
    into: {
      title: 'Pickers: named chips and Show more',
      category: 'The planning conversation', screen: 'Planning conversation', work: 'pending',
      question: 'Which chips can a turn name, and what sits behind Show more?',
      context: 'You approved mp-272 (a turn may name the chips it expects next) and mp-230 (Show more opens many more options from the same search). Ticket 31 built both.',
      decision: L([
        'A turn\'s named chips replace only the two reply chips ("I like these", "Other options"). The doors and filters stay. Chips belong to a picker, so a turn without a picker names none. (mp-406, mp-409) [built]',
        'Both the server and the app trim the list to two to four chips. A broken list means the app shows its own set. (mp-408) [built]',
        'Show more opens the rest of the same search, up to 24 more meals, with no new query. (mp-407) [built]',
        'Meals behind Show more count as shown only if the athlete opened the sheet. (mp-410) [to build]',
        'When nothing is left behind Show more, the chip is not drawn. 24 is enough. (mp-411) [no work]',
        'A tick in the Show more sheet shows on the tiles behind it at once, and the other way round. (mp-412) [to build]',
      ]),
      why: 'Clauses 1 to 3 are built. Clause 6 is the one an athlete would notice.',
      alternatives: 'Marking all 24 meals as shown whether or not the sheet was opened, which starves later suggestions.',
      touches: 'The picker part, the Show more sheet, the planning conversation.',
    },
  },
  {
    from: ['mp-329', 'mp-345', 'mp-331'],
    answers: ['mp-330', 'mp-346', 'mp-332'],
    into: {
      title: 'Meal reviews, recipe origin labels and typed feedback',
      category: 'Meals tab and library', screen: 'Meal detail', work: 'pending',
      image: 'docs/ssot/decisions/images/mealplanning/meal-detail.png',
      question: 'Are the three small builds from wave 1 and 2 right: admin meal reviews, origin labels on recipes, and typed feedback reaching Wiredash?',
      context: 'Tickets 25, 26 and 32 each built something you had already approved (mp-144 clause 3, mp-245 clause 6, mp-146) and each left one question.',
      decision: L([
        'An admin sees a review box on every meal: Good recipe or Not good, a reason, Send. Reviews wait for the server before saying sent. Only admins can read them. (mp-329) [built]',
        'The team reads reviews in the database for now. Each row gets the app version. No screen until there are enough reviews to need one. The table goes to production with the cutover. (mp-330) [to build: the version stamp]',
        'A recipe says where its steps came from in one line under Directions: "As published by X" with a link, "Steps from X", or "A simple assembly, no recipe needed". AI-written steps keep the sparkle. (mp-345) [built]',
        'A published recipe links the original once, from the new label. The older "See the original recipe" row goes, which also removes the overflow on narrow phones. (mp-346) [to build]',
        'Feedback typed to Vana is filed to the same Wiredash inbox as a shaken report, from the device, with the label "vana-chat" so the inbox can filter it. (mp-331, mp-332) [built, except the label]',
        'Wiredash has no public way to file an entry without its screen, so the app uses the SDK\'s internals with the version pinned. Ask Wiredash for a public call. Every Wiredash upgrade re-checks the filer. (mp-332) [no work]',
      ]),
      why: 'All three are small and already on dev.',
      alternatives: 'Building a review screen now. Keeping both links to the original recipe.',
      touches: 'Meal detail, the meal_reviews table, the recipe screen, Vana\'s feedback tool, Wiredash.',
      details: 'Review migration 20260916120000 on dev. Reason capped at 2,000 characters. Wiredash metadata: source vana_chat, sentiment, about, conversation id, user id and email.',
    },
  },
  {
    from: ['mp-327', 'mp-388'],
    answers: ['mp-322', 'mp-325', 'mp-328', 'mp-339', 'mp-354', 'mp-355', 'mp-356', 'mp-357', 'mp-383', 'mp-395', 'mp-405', 'mp-413', 'mp-414', 'mp-415', 'mp-338'],
    into: {
      title: 'Housekeeping: fifteen questions that need no product ruling',
      category: 'Process and scope', screen: 'none (algorithm/data)', work: 'pending',
      question: 'Can the engineering loose ends the build agents raised be closed in one go?',
      context: 'Build agents are told to raise anything they are unsure of. Fifteen of those questions are about process, tests and tidying, not the product. You said on 09-16 not to park things for your decision, so each gets an answer here. Tick any clause you disagree with.',
      decision: L([
        'The dev-tools switch sits in a "Dev build" card on Settings, remembered per device. The card moves up so the floating buttons do not cover it. (mp-327, mp-328) [built, except the move]',
        'The coach-insight panel is gone and nothing writes insights any more. The old columns and their readers are removed with the production cutover. (mp-388, mp-395) [to build]',
        'A check only a person with a phone can do (a sandbox purchase, a second account) does not fail a ticket. The ticket closes on its tests and the check goes on your simulator and device list. (mp-322) [no work]',
        'You declined the sandbox wizard on 09-16. Purchase and Restore are checked by hand on TestFlight before a release (mp-336). The two wizard questions are closed. (mp-354, mp-355) [no work]',
        'Team accounts get in through the admin flag you approved (mp-416). An account that also needs Vana\'s server calls gets a promotional grant in RevenueCat, and the RevenueCat dashboard is the list. This also lets a wave simulator past the paywall. (mp-338, mp-415) [no work]',
        'The odd entitlement row on dev was written by hand by a build agent for the test user. It is deleted, and agents are told the webhook is the only writer. (mp-357) [to build]',
        'The internal-device flag is written again by the tester switch (meal photos, 09-16), so Mixpanel keeps excluding the team. (mp-339) [no work]',
        'The release doc still describes a dark launch behind a flag. It is rewritten to the trial-and-purchase release. (mp-356) [to build]',
        'Design specs the app changed (the sheet, the launcher section, the meal mosaic) are written app-side as "proposed, awaiting Xuan", which is the rule you set on 09-11. Xuan is sent the list. (mp-325, mp-383) [to build: the list]',
        'The Show more sheet takes the glass-sheet material like every other summoned sheet. (mp-414) [to build]',
        'The persona is edited in this repo only. The note about keeping the prototype in step is deleted. (mp-413) [to build]',
        'The fuelling conformance test that goes red and green without a code change is time-dependent. It gets a fixed clock. (mp-405) [to build]',
      ]),
      why: 'None of these changes what an athlete sees, except the glass sheet. They were making the page look heavier than it is.',
      alternatives: 'Ruling each separately.',
      touches: 'Settings, the release doc, the dev entitlement table, the design specs list for Xuan, the conformance suite, persona.ts.',
      details: 'Dev-tools key dev.tools_visible. The eval entitlement row: user 37129f7e, period_type "eval", written 2026-09-15 19:05Z.',
    },
  },
];

export const fresh = [
  {
    answers: ['mp-287'],
    into: {
      title: 'Adopt Xuan\'s RevenueCat spec for the 1 October paywall',
      category: 'Pro and paywall', screen: 'Paywall', work: 'pending',
      question: 'Do we build the paywall to Xuan\'s RevenueCat spec, including the three places it changes what you ruled on 09-15?',
      context: 'Xuan\'s spec (docs/revenuecat-spec-for-lee.md, revised 09-16) is the paywall document mp-286 and mp-287 were waiting for. It sets prices, the Founding Month from 1 October to 30 November, how coaches get in, and a ship order with store submission around 25 September. It differs from three approved cards: mp-279 clause 1 (no new product ids), mp-283 (no grace for existing accounts) and mp-280 (what a lapsed account can open). Her answers are adopted here; tick a clause to keep your earlier ruling instead.',
      decision: L([
        'Prices: $24.99 a month and $199.99 a year. Founding Month prices, 1 October to 30 November: $12.49 and $99.99. Each with the seven-day free trial. These are new product ids (me_pro_…), which reverses mp-279 clause 1. [to build]',
        'Four products now (base and founding). The coach-discount products (15% and 30% off, 14-day trial) wait until mid-October because founding beats them until 30 November. [to build]',
        'RevenueCat shows the founding offering to everyone during the window. It is switched on by hand on 1 October and off on 30 November. [to build]',
        'A coach registers like anyone else and enters their own code. The server sees the code is theirs, marks the account as a coach and grants 30 days of access at once, so a coach never sees the paywall. Another 30 days when their first athlete pairs, then free for as long as five of their athletes are active. Through November this is done by hand in the RevenueCat dashboard. This answers mp-287 and replaces mp-286. [to build]',
        'Every account that exists when the paywall switches on gets 30 days of granted access and founding-member status, announced by email a week before. This reverses mp-283. [to build]',
        'An account with no access sees its data read-only with the paywall over it, rather than nothing at all. This adjusts mp-280. [to build]',
        'On day five of the trial the app sends a push: the trial ends in two days, the price after, and how to cancel. It is scheduled on the phone at purchase, so it needs no server work. [to build]',
        'Discount and coach codes are ours, in our own table. Apple\'s and Google\'s offer codes are not used. [to build, by 1 October]',
        'Before any product is created, RevenueCat support is asked one question: one subscription group for all eight products, or a separate group for the discounted ones. [you or Xuan]',
        'Cut order if time runs short: coach-discount products, then the coach cron, then giveaway grants, then the webhook-to-Mixpanel pipeline. Never cut: products, paywall, trial, the day-five reminder, the grace grant, Restore. [no work]',
      ]),
      why: 'Xuan owns pricing and go-to-market, and her document is the one the 09-15 rulings said they were waiting on. The dates leave five days to store submission.',
      alternatives: 'Keeping the 09-15 rulings as they are: existing product ids, no grace period, everything locked for a lapsed account.',
      touches: 'App Store Connect, Play Console, RevenueCat offerings, the paywall, the gate, a codes table, OneSignal, the legacy-grant run, mp-279, mp-280, mp-283, mp-286.',
      details: 'You rejected mp-319 (store offers by script) on 09-17 and asked for the store setup to be done through the browser instead. Apple gives one introductory offer per subscription group per person, so a lapsed seven-day trial cannot later get the 14-day coach trial. All eight products in one group would let any subscriber switch to a founding price from Apple\'s own Manage Subscriptions screen; removing founding products from sale on 30 November closes that.',
      source: 'docs/revenuecat-spec-for-lee.md (Xuan, revised 2026-09-16)',
    },
  },
  {
    answers: ['mp-320', 'mp-343', 'mp-344'],
    into: {
      title: 'The AI allowance, the caps and the cost work',
      category: 'Pro and paywall', screen: 'none (algorithm/data)', work: 'pending',
      question: 'How much Vana does a subscriber get each month, what stops abuse, and what is done to bring the cost down?',
      context: 'On 09-17 you rejected mp-341 and asked for a thorough look at cost and pricing: the most Vana we can give for the monthly price while keeping a healthy profit, and ways to cut cost such as Apple\'s on-device model or routing simple prompts to a cheaper model. The research is in docs/research/vana-cost-and-pricing.md and was re-measured on dev on 09-20. A planning turn costs about 3.2 cents today, twice what mp-340 assumed; a typical athlete costs about $2.86 a month and a heavy one $6.85. After the cache fix those fall to $1.64 and $3.93. At Xuan\'s prices the app keeps $10.62 a month from a founding monthly subscriber and $7.08 from a founding annual one.',
      decision: L([
        'One credit is one AI action: a message to Vana, a described meal, a meal photo. Openers are free. Weighting by cost would charge most for planning, which is what the subscription sells. [built]',
        'The monthly allowance is 600 credits, up from the 300 in mp-340, and the cache fix ships before the paywall opens. 600 covers a heavy user (about 494 a month) and a typical user spends a third of it. If the cache fix is not live by 1 October the allowance opens at 400 and moves to 600 when it is. [to build: one setting]',
        'The trial week gets 150 credits, not the full allowance. More than half of cancelled trials cancel on day one. This amends mp-281 clause 3. [to build]',
        'Unused allowance does not roll over. Bought credits never expire, which Apple requires. (mp-281, unchanged) [built]',
        'The allowance sits in the same wallet as bought credits and is spent first. Monthly plans refill on renewal. Annual plans refill each month on the anniversary day, checked when the app opens and before any AI call. A lapsed subscription forfeits what is left of the allowance; cancelling does not, until the period ends. This is mp-341\'s mechanism, which is already live on dev. [built]',
        'At zero credits the athlete gets the top-up sheet and Vana stops (mp-282). Later, once a cheaper model passes the voice test, general chat carries on with it and only planning stops. [built; the fallback is later]',
        'Top-up packs stay at 50 credits for $4.99 and 250 for $19.99. Margin is 53% or better even at today\'s cost. [built]',
        'Daily caps against scripts: 40 openers and 150 chat turns per account per day. Openers have no working limit today, and one account could run up $40 to $60 a day. [to build, before 1 October]',
        'Every AI function checks the subscription on the server, not only the four Vana ones. Today a lapsed account holding bought credits can still call describe-meal, meal photo, the coach insight and the old chat. (mp-320) [to build, before 1 October]',
        'The old free grant of 20 credits a month ends when the paywall opens. Free credits already in wallets stay. (mp-343) [to build]',
        'When a subscription is transferred to another account the allowance stays where it was granted. Switching between monthly and annual grants nothing until the next refill. (mp-344) [built]',
        'Cost work, in this order: log what is missing, the cache fix, the caps, byte-stable replay, openers that fetch their data before calling the model, then a 50-meal test to move meal logging off Sonnet to the cheapest model that passes. [to build]',
        'Not doing now: Apple\'s on-device model (a 4,096-token window, cannot run a Vana turn, would save under 5%), the batch API (a day\'s delay breaks the next opener), and routing simple prompts to a cheaper model until it passes a test of Vana\'s voice (about 6%). [no work]',
      ]),
      why: 'At 600 credits after the cache fix, a subscriber who spends every credit on planning turns costs about $7, against $10.62 kept from a founding monthly plan. The typical subscriber costs about $1.64, 15% of what the app keeps. The one thin spot is the founding annual plan at $7.08 a month.',
      alternatives: 'Staying at 300 (a heavy user runs dry around day 18). No visible meter with a silent slow-down, which needs the cheaper fallback model first. Unlimited with rate limits only, which leaves no monthly bound.',
      touches: 'The allowance setting, the webhook grant, the rate limits, the server gate on describe-meal, analyze-meal-photo, ai-coach and jade-chat, the call log, mp-281, mp-340, mp-341.',
      details: 'Dev, last 30 days, Haiku 4.5 through the Vercel AI Gateway (no markup): planning turn 21.3k tokens in, $0.032 average, $0.055 at p95; general turn about $0.011; planning opener $0.015; general opener $0.010; describe-meal (Sonnet 4.6) $0.007; meal photo $0.013; coach insight $0.002. Net after a 15% store cut: $24.99 -> $21.24, $12.49 -> $10.62, $199.99 a year -> $14.17 a month, $99.99 a year -> $7.08. Worst case 600 credits all on planning: $19.20 today, $6.96 after the cache fix. Profiles are assumptions from 3 to 8 dev users; October traffic replaces them. Two wallet bugs to fix with the caps: the credit check passes when the database errors, and a balance that drains between check and debit serves one free call.',
      source: 'docs/research/vana-cost-and-pricing.md (2026-09-17); dev cost audit 2026-09-20; Lee on the page 2026-09-17 rejecting mp-341',
    },
  },
];
