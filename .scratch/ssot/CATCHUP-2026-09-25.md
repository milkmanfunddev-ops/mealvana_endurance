# SSOT catch-up after testing-wave fix waves 19-24 (brief, 2026-09-25)

Lee (09-25): the testing waves no longer write to the decisions page; the SSOT is brought up to
date later, in one pass, from the testing documents. This is that pass. It runs in parallel with
a testing-wave retest session (`.scratch/testing-wave/HANDOFF.md`), so it follows the rules below.

## Scope and rules

- Run `/ssot backfill mealplanning` (`.claude/skills/ssot/SKILL.md`), with the prologue first. The
  testing wave's product questions and rulings live in the mealplanning record
  (`docs/ssot/decisions/mealplanning.md`, proposals `.scratch/mealplanning/decisions.md`).
- Sources for this pass: fix tickets 33-85 in `.scratch/testing-wave/issues/` (their What to build,
  Decisions lines, and the notes the lead added), the Findings they name in
  `.scratch/testing-wave/findings/`, the wave close commits (`git log --grep "wave 1[9]\|wave 2[0-4]" --oneline`),
  and `.scratch/testing-wave/IMPROVEMENTS.md` only for what it says about product behaviour.
- **Product decisions only** (memory: SSOT is product decisions only): no cards for harness,
  test process, build order, test seams or implementation detail.
- Do not touch the testing-wave session's files: nothing under `.scratch/testing-wave/`, no
  simulators, no app builds, no edits under `lib/`, `test/` or `supabase/`. This pass writes only
  `.scratch/mealplanning/decisions.md`, `docs/ssot/decisions/` (through `sync.mjs` as the README
  says), `CONTEXT.md`, the mealplanning spec's citations, and the page.
- Commit with explicit paths, `[skip ci]`; never `git stash`; other sessions commit on the same
  branch. Push `mealplanning` only if Lee says so (credentials: `GIT_ASKPASS` script running
  `gh auth token --user lbm54`).
- Newest approval wins on contradictions (Lee). Page writes need `if_version`; never full-reseed.

## Known drift and rulings to put on the page

1. **mp-674..mp-679 are Lee's own rulings from 09-25, still `proposed`.** Ask Lee in the terminal
   whether to mark them approved as spoken; they are built (tickets 61, 67, 70, 71, 73, 74, 75, 77).
2. **mp-241's New meal plan clause** ("'New meal plan' archives the plan it is on") is superseded
   by mp-674 (the plan stays until the new one is confirmed). Propose the amendment.
3. **mp-609** (renewal / webhook): wave 19 (ticket 38) added `user_entitlements.will_renew` and a
   15-minute server grace (`RENEWAL_GRACE_MS`, only when the subscription renews), and closes at
   min(expiry, event time). Its clauses 1/3/4 no longer match the code. Propose the amendment;
   ticket 38 is one answer to mp-665.
4. **mp-661** (redemptions on account delete): ticket 39 made `code_redemptions.user_id`
   `ON DELETE SET NULL`, so a redemption outlives the account. Propose it as mp-661's answer.
5. **mp-679 read as "renewing only"**: the phone's saved copy gets the 15 minutes only when it
   will renew, as the server (wave 22 review). Wave 24 (ticket 77) also closes the Gate on a timer
   while the app stays open (Finding 05-005). Propose one card stating both.
6. **mp-675 built partly**: edit servings, remove, rename, delete, Use this plan again; adding new
   meals is open as mp-681. Pre-09-16 archived plans drop off Previous plans (no record of
   whether they were confirmed); note it on the card.
7. **mp-678 as built**: blank-number meals stay in Browse with Add unavailable and a message; the
   Swap list leaves them out; `set_day_slot` and every add path refuse them (tickets 61, 74).
8. **Wave 24 product behaviour worth a card each** (read the tickets for the exact copy):
   Delete Account says a renewing store subscription keeps running (78); notification permission
   is asked right after sign-in, never before Welcome, and speech only on the first mic tap (79);
   a returning subscriber is welcomed back (80); Previous lists marks "This week's plan" and plan
   lists are named "Week of Sep 20" (84); Disconnect Kroger asks first (82); the AI's note is
   saved as the meal's note, in the same field the athlete edits (83).
9. **Open questions already on the page, leave open unless Lee answers**: mp-660, mp-669,
   mp-680, mp-681, mp-682, mp-683.
10. **For Xuan, not the page** (QA repo owns the spec): Finding 30-005 (pre-workout timing label;
    SSOT B-2, I6, PW-021 forbid a clock-relative label) and the during-run band clamp (ticket 63,
    3 vectors skipped). List them in the report for Lee to pass on.

## Also in this pass (docs, no page)

- `CONTEXT.md` **Lapsed** already reads full-screen paywall, no read-only (mp-280); check the
  glossary for any other term the waves changed (Draft, Previous plans, Grant grace) and fix it.
- CLAUDE.md's controller rule allows a rethrow only for Meal photos, but `MealPlanController._remoteAck`
  and `EarlierPlan._send` (ticket 73) also rethrow a remote-ack failure. Tell Lee; propose the
  one-line CLAUDE.md wording, do not edit CLAUDE.md without his yes.
- `.scratch/mealplanning/spec.md` citations: run the prologue's step 6 (`sync.mjs cite`).
