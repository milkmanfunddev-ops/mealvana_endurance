# 141: Accessibility, dev buttons, small fixes

**Status:** in-progress (wave 41, 2026-09-26)
**Blocked by:** none.
**Next:** `/implement-lee testing-wave`
**Model:** fable

**What to build:** Fixes from the 2026-09-26 triage: one VoiceOver sweep, the dev buttons folded into one, and four small fixes. One agent, as a list. Onboarding gets accessibility labels only: never change its body copy (Xuan's). Button and dialog labels may change.
1. **VoiceOver sweep (100-007, 118-005, 118-006, 119-005, 119-006, 124-005).** Every tappable control becomes a named button, every choice reports its selected state, and every text field keeps its name once it holds a value. The list, by screen:
   - Onboarding: the back arrow gets "Back". The full-screen "Continue" element on "Tell us about yourself" goes away. Sport and obstacle tiles are buttons with a checked state. The first and last name fields keep their names once filled.
   - Sign Up with Email: both eye buttons get "Show password", as Log In's does (118-005, 119-005).
   - New Event and the Log In chooser: the back arrow is a button named "Back". On the chooser it is not in the tree at all (124-005).
   - Vana full screen: Back, New conversation, Conversations, Add and Dictate. Browse meals: Back, Search meals and Filters. All become buttons, and the filter menu items reach 48 pt.
   - Connected Apps: Reconnect and Sync Now become their own buttons, not parts of one image label.
   - Profile & Preferences: the bottom back arrow gets "Back". The "tap to use" chips are buttons. Gender, units, gut-training and sweat options report selected. Email and Birthday have names. The theme dialog's System / Light / Dark report selected, and Nutrition Targets fields are named by their target, not "Auto" (119-006).
   - Review & Log: the photo thumbnail gets a label, for example "Photo being logged" (100-007).
   - Paywall: the message's semantics box ends where the drawn message ends. The bottom clearance moves outside its `Semantics`, so a touch on Continue or the plans reads those controls (118-006).
2. **One dev button at the top edge (100-001, 118-001, IMPROVEMENTS #98).** The blue "Open testing tools" wrench and the red "Show accessibility issues" checker become one small button at the top edge, just under the status bar. A tap offers both tools. They cover bottom buttons and row menus today (`dev_testing_tools.dart` raises the bottom safe area to put them above the Vana launcher). The button stays visible on dev, with no hide flag (CLAUDE.md). In debug, `accessibility_tools` 2.2.3 draws its own two buttons in `buttonsAlignment` (topLeft to bottomRight, no hide option). Folding them into one likely means mounting the checker overlay and the panel from the package's `src/`, as the release path already does for the panel (unverified). Drop the bottom-padding trick once nothing sits at the bottom. Check the new spot clears the date header and every back button.
3. **Notify Me answers (117-006).** Learn's Notify Me (`coming_soon_section_widget.dart:102`, `onPressed: null`) records interest and says "We'll let you know". A second tap writes nothing. Record it the way Connected Apps' Notify Me does (`_notifyProvider` in `connected_apps_screen.dart`: an analytics event plus a notified state). Both Learn cards use the widget.
4. **Event countdown counts months fairly (117-007).** "1 month away" for 58 days: `(days / 30).floor()`. Round to the nearest month, or show weeks under about 9 weeks, so 58 days reads "8 weeks away". The same formula is copied in `event_countdown_badge.dart`, `upcoming_event_widget.dart` and `upcoming_event_card_kyle.dart`. Use one shared function for all three.
5. **Learn offline is not "no videos" (117-008).** When the education fetch fails (`education_repository.dart`), `education_screen.dart:93` shows "No videos available yet". Show the last loaded lessons if a cache exists, or an offline message with Retry. Keep the empty state for a successful empty answer.
6. **Dictation message names the app and clears the composer (120-005).** `meal_planning.mic_permission` says "iOS Settings → Mealvana". The dev build is "Endurance Dev" in iOS Settings. Fill the app's display name in at runtime (for example `PackageInfo.appName`, unverified on iOS). Place the snackbar so the composer stays tappable while it shows.

**Findings:** 100-007, 118-005, 118-006, 119-005, 119-006, 124-005, 100-001, 118-001, 117-006, 117-007, 117-008, 120-005.

**Decisions:** none on the page. Lee's rulings in the terminal (`triage-20260926.md`): onboarding labels only, dev buttons fold into one at the top edge, Notify Me records and confirms, and the dictation message names the app as iOS lists it.

**Touches:** lib/shared/widgets/dev_testing_tools.dart, lib/shared/widgets/root_app_widget.dart, lib/features/onboarding/presentation/widgets/onboarding_step_scaffold.dart, lib/features/onboarding/presentation/widgets/onboarding_navigation_footer.dart, lib/features/onboarding/presentation/widgets/onboarding_multi_select_step.dart, lib/features/onboarding/presentation/screens/onboarding_pageview_screen.dart, lib/features/auth/presentation/screens/email_signup_screen.dart, lib/features/auth/presentation/screens/post_onboarding_auth_screen.dart, lib/features/events/presentation/screens/event_form_screen.dart, lib/features/meal_planning/presentation/screens/vana_chat_screen.dart, lib/features/meal_planning/presentation/screens/vana_browse_screen.dart, lib/features/settings/presentation/screens/connected_apps_screen.dart, lib/features/integrations/presentation/widgets/integration_provider_card.dart, lib/features/settings/presentation/screens/preferences_screen.dart, lib/features/settings/presentation/screens/settings_screen.dart, lib/features/settings/presentation/screens/nutrition_targets_screen.dart, lib/shared/widgets/kyle_design/data/kyle_source_chip.dart, lib/features/meal_logging/presentation/widgets/meal_photo_thumbnail.dart, lib/features/subscription/presentation/screens/paywall_screen.dart, lib/features/education/presentation/widgets/coming_soon_section_widget.dart, lib/features/education/presentation/screens/education_screen.dart, lib/features/education/data/education_repository.dart, lib/features/events/presentation/widgets/event_countdown_badge.dart, lib/features/events/presentation/widgets/upcoming_event_widget.dart, lib/features/events/presentation/widgets/upcoming_event_card_kyle.dart, lib/features/meal_planning/presentation/widgets/vana_mic_button.dart, lib/features/content/domain/content_keys.dart, assets/config/content_defaults.json

The onboarding back arrow and tile widgets are mapped from code, not checked on screen (unverified). Follow each control from its screen's route before editing. The theme dialog lives in `settings_screen.dart`.

- [x] Widget tests (`meetsGuideline(labeledTapTargetGuideline)` and `androidTapTargetGuideline` where they fit, plus semantics finders): each control in item 1 has its name, role and selected state. A filled name field keeps its label. The paywall message's semantics rect does not contain Continue's centre.
- [x] Widget test: the dev tools mount one button, inside the top safe area plus a small offset. No tools button hits a tap at the Welcome Build My Plan or paywall Continue right end.
- [x] Unit test: the shared countdown gives "8 weeks away" for 58 days and months beyond. Widget tests: Notify Me confirms once and is inert on a second tap; Learn with a failed fetch shows the offline state, not "No videos available yet"; the mic message carries the display name.
- [x] `flutter analyze` clean on touched files.

Next: /implement-lee testing-wave
