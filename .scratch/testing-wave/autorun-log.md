# Autorun log (2026-09-26, /loop /implement-lee testing-wave)

- Ticket 142 (harness) done by the lead's agent, merged `4efee299`. New: `netcut slow`, `on` closes open sockets, `seed-states.mjs` (pairing, 2-minute grant), `CRED type` 2 s wait. Open: RevenueCat accepts only one grant per account (200 with no write on a second); IMPROVEMENTS #100 (2 s after typing is not always enough).
- Wave 41 (135, 137, 141): all merged, landed `bb98f9ea`. Dev: migration `20260926163500_meal_logs_servings`; functions generate-nutrition-plan-v3, generate-macros-v4. Review: 1 blocker fixed (mixed-batch NULL servings), 3 minor noted in 135.
- Pre-existing red, not a wave's: `test/shared/ci_config_contract_test.dart` "same flow list" fails since develop merge `badfe725` (carb_loading_ripple flow is in the M1 list, not in Codemagic's `integration-tests-develop`). A CI-config change: left for Lee.
- Wave 42 (133, 136, 139): all merged, landed `4db57601`. Dev: migrations `20260926163300_drop_draft_lists` (4 rows), `20260926163900_users_admin_flags_server_only`; functions analyze-meal-photo, describe-meal, jade-chat, kroger, meal-photo, vana-action, vana-chat, vana-day-notes, discard-signup (new). Review: security blocker fixed (the admin trigger read an empty setting, so any athlete could make themselves admin and get Pro). Merge found: `content_defaults.json` passed 50 KiB, where `loadString` goes through `compute()` and every widget test read raw keys; both loaders now decode bytes directly.
- Wave 43 (134, 138, 140): all built, merged, reviewed and green on branch `merge/wave-43` (`7dd72407`), NOT yet on `mealplanning`: another session holds uncommitted edits to `paywall_screen.dart` and its test, which 140 also changes, so the fast-forward would overwrite them. Land with `git merge --ff-only merge/wave-43` once that session commits (re-merge `mealplanning` into the branch first if it conflicts), then `SYNC wave testing-wave .scratch/testing-wave/issues/ --close 43 --merged 134,138,140 --suite green` and delete the wave branches. Waves.json still shows 43 open.
  - Dev (already live): migration `20260926163400_vana_action_requests`; day_notes_stale set on plans 666be167, 8ebeb6da; functions delete-user, garmin-backfill, garmin-deregistration, garmin-ping, garmin-push, garmin-user-mapping, generate-nutrition-plan-v3, jade-chat, kroger, redeem-code, revenuecat-webhook, vana-action, vana-chat, vana-day-notes (vana set redeployed after merging the other session's short-opener persona change).
  - Review: blocker fixed (a request id never expired, so a later real second serving replayed an old result and logged nothing: ids now last 2 min). Also: conversation-kind lookup fails fast offline; Garmin deregistration times out at 10 s. Merge fix: a same-second profile edit lost its upload (03-009 regression), now compared by payload.
  - Not done: 140 item 6 (Subscription screen `will_renew`) needs an App Store sandbox cancel on a device. Garmin dev orphans dad6fb42/1df3fb7b cannot be deregistered: no mapping or token left on dev (needs Garmin partner support; pushes now log once as skipped). No code-made pending pairings existed to flip.
- Retest tickets drafted (19, 143-161, 178 checks, 200 Findings) in `retest-drafts/`, not in `issues/`, so they are not on the frontier. Gap: fix tickets 126-132 never got retests (32 triaged Findings).
- Stopped here at Lee's request (2026-09-26), before the app rebuild and the retest waves.
