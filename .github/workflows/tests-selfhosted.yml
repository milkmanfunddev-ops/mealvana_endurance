# Tests on a self-hosted macOS runner (Lee's Mac mini M1).
#
# Split CI: Codemagic keeps doing build/deploy (signing, TestFlight, Play,
# Shorebird). This workflow runs the TEST suites on owned hardware to save
# Codemagic minutes. Results show as PR status checks in GitHub.
#
# Requires:
#  - A self-hosted runner registered on the M1 with labels: [self-hosted, macOS, ARM64]
#    (scripts/setup_m1_runner.sh sets this up).
#  - Repo secrets:
#      DOTENV_ROOT / DOTENV_DEV_LOCAL   — mirrors of the gitignored .env files
#                                         (bundled as Flutter assets)
#      DOTENV_WEB_LOCAL                 — .env.web.local, must be valid JSON
#                                         (flutter drive --dart-define-from-file)
#      INTEGRATION_TEST_EMAIL/PASSWORD  — dev-project test account; without these
#                                         every Patrol flow SELF-SKIPS instead of
#                                         actually exercising the app.
#
# ---------------------------------------------------------------------------
# WHY THIS WORKFLOW KEPT FAILING (2026-07, fixed here)
#
#  1. Every failure was "The self-hosted runner lost communication with the
#     server" at *exactly* 10m00s — GitHub's runner-offline timeout — while the
#     steps themselves kept grinding for another 8+ minutes. The M1 has 8 GB RAM
#     and was at 99% disk; `dart run build_runner build` (4k+ inputs) plus
#     `flutter analyze` swapped the box hard enough that the runner agent missed
#     its heartbeat. Preflight now hard-fails on low disk instead of thrashing.
#  2. The Patrol job pinned `-d "iPhone 15 Pro Max"`, which does not exist on
#     this machine (only iOS 26.2 devices are installed).
#  3. The Patrol job passed `-t lib/main_dev.dart`. For patrol_cli, `-t` IS
#     `--target`, so that registered main_dev.dart as a test target and the CLI
#     bailed with `target lib/main_dev.dart is invalid`.
#  4. `dart pub global activate patrol_cli` (unpinned) installs whatever is
#     newest, which need not pair with the `patrol` package in pubspec.lock.
#     Pin it. A mismatched pair fails at link time with undefined XCTest symbols
#     in PatrolImpl.o, not with a version warning (2026-09-16).
#
# Also: macOS `pmset sleep` was 1 minute on this box. Run once, with sudo:
#     sudo pmset -a sleep 0 disksleep 0 womp 1
# ---------------------------------------------------------------------------
#
# Trigger model: the full suite runs on EVERY push and PR. The whole point of
# owning the hardware is that running everything costs nothing but wall-clock.
name: Tests (self-hosted M1)

on:
  push:
  pull_request:
  workflow_dispatch:

# One in-flight run per ref; newer pushes cancel older. Important on a single
# self-hosted runner — otherwise pushes queue up behind each other.
concurrency:
  group: tests-selfhosted-${{ github.ref }}
  cancel-in-progress: true

env:
  # Every installed simulator is iOS 26.2. Keep this in sync with the runner.
  PATROL_DEVICE: iPhone 17 Pro
  # patrol_cli 4.8.0 <-> patrol 4.10.x. See the compatibility table:
  # https://patrol.leancode.co/documentation/compatibility-table
  PATROL_CLI_VERSION: 4.8.0
  # Below this the box swaps hard enough to kill the runner's heartbeat.
  MIN_FREE_DISK_GB: 25
  # The Patrol targets and the expected case count are derived, not listed:
  # scripts/patrol-targets.mjs globs integration_test/ and integration_test/flows/
  # for *_test.dart minus integration_test/runner_exclusions.json (flows that
  # spend on AI, need a clean install, or need interactive OAuth, each with a
  # reason). A new flow joins this job by existing; do not edit this file for it.

jobs:
  # Device-less suites — fast, reliable, no Xcode/simulator needed.
  unit-web-deno:
    name: Unit + Web e2e + Deno
    runs-on: [self-hosted, macOS, ARM64]
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - name: Preflight (keep awake, verify headroom)
        run: |
          set -euo pipefail
          # Keep the Mac from sleeping mid-job — a sleeping runner reads to
          # GitHub as "lost communication". Detached so it outlives this step.
          nohup caffeinate -dimsu -t 7200 >/dev/null 2>&1 &
          free_gb=$(df -g /System/Volumes/Data | awk 'NR==2 {print $4}')
          echo "Free disk: ${free_gb} GB (minimum ${MIN_FREE_DISK_GB} GB)"
          if [ "$free_gb" -lt "$MIN_FREE_DISK_GB" ]; then
            echo "::error::Only ${free_gb} GB free. Reclaim space (unused simulator runtimes, DerivedData, build/) — below this the runner gets starved and drops its heartbeat."
            exit 1
          fi

      # Secrets go through `env:`, NOT `${{ }}` inside the script. Actions
      # substitutes `${{ secrets.X }}` textually before the shell parses the
      # line, so a secret containing double quotes — DOTENV_WEB_LOCAL is JSON —
      # terminates the surrounding shell string and writes a mangled file. That
      # is what made web-e2e fail with "not valid JSON" even though the stored
      # secret was fine.
      - name: Write gitignored .env files from secrets
        env:
          DOTENV_ROOT: ${{ secrets.DOTENV_ROOT }}
          DOTENV_DEV_LOCAL: ${{ secrets.DOTENV_DEV_LOCAL }}
          DOTENV_WEB_LOCAL: ${{ secrets.DOTENV_WEB_LOCAL }}
        run: |
          printf '%s' "$DOTENV_ROOT"      > .env
          printf '%s' "$DOTENV_DEV_LOCAL" > .env.dev.local
          printf '%s' "$DOTENV_WEB_LOCAL" > .env.web.local
          : > .env.prod.local   # empty stub; asset bundler just needs the file
          test -s .env.web.local || { echo "::error::DOTENV_WEB_LOCAL secret is empty"; exit 1; }
          python3 -c 'import json,sys; json.load(open(".env.web.local"))' \
            || { echo "::error::DOTENV_WEB_LOCAL must be valid JSON — flutter drive parses it with --dart-define-from-file"; exit 1; }

      - name: Flutter deps
        run: flutter pub get

      - name: Code generation
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Analyze (non-fatal — reports lints, fails only on errors)
        run: flutter analyze --no-fatal-warnings --no-fatal-infos

      - name: Unit + widget tests
        run: |
          mkdir -p test-results
          flutter test --exclude-tags="integration || e2e"

      - name: Nutrition algorithm tests (Deno)
        run: bash supabase/functions/run-algorithm-tests.sh

      - name: Web e2e (flutter drive boot smoke)
        run: bash scripts/run_web_e2e.sh

  # Patrol integration flows on iOS. Runs via patrol_cli (NOT `flutter test`) —
  # patrol builds an XCUITest host, boots the sim, and drives the dev flavor.
  # Android Patrol is intentionally deferred (needs Android SDK 36 + AVD).
  #
  # `needs:` is deliberate: this box has one runner and 8 GB of RAM. Letting the
  # Xcode build overlap with build_runner/analyze is what starved the agent.
  integration-patrol-ios:
    name: Patrol (iOS)
    # `needs` keeps the two jobs SERIALIZED (one runner, 8 GB RAM — overlap
    # starves the agent). `if: !cancelled()` lets Patrol still run when the
    # unit job is red — since 2026-08-18 the unit job has been failing on two
    # macro-dashboard GOLDEN pixel diffs (0.12%, host-rendering delta between
    # the machine that generated the goldens and this M1), which silently
    # blocked the whole integration suite. Fix the goldens properly (per-host
    # regen or a tolerance comparator) — then this guard never matters.
    needs: unit-web-deno
    if: ${{ !cancelled() }}
    runs-on: [self-hosted, macOS, ARM64]
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - name: Preflight (keep awake, verify headroom)
        run: |
          set -euo pipefail
          nohup caffeinate -dimsu -t 7200 >/dev/null 2>&1 &
          free_gb=$(df -g /System/Volumes/Data | awk 'NR==2 {print $4}')
          echo "Free disk: ${free_gb} GB (minimum ${MIN_FREE_DISK_GB} GB)"
          if [ "$free_gb" -lt "$MIN_FREE_DISK_GB" ]; then
            echo "::error::Only ${free_gb} GB free — an Xcode simulator build needs more headroom than this."
            exit 1
          fi

      # See the note in the other job: secrets via `env:`, never `${{ }}` inline.
      - name: Write gitignored .env files from secrets
        env:
          DOTENV_ROOT: ${{ secrets.DOTENV_ROOT }}
          DOTENV_DEV_LOCAL: ${{ secrets.DOTENV_DEV_LOCAL }}
        run: |
          printf '%s' "$DOTENV_ROOT"      > .env
          printf '%s' "$DOTENV_DEV_LOCAL" > .env.dev.local
          : > .env.prod.local

      - name: Write Patrol test credentials
        env:
          TEST_EMAIL: ${{ secrets.INTEGRATION_TEST_EMAIL }}
          TEST_PASSWORD: ${{ secrets.INTEGRATION_TEST_PASSWORD }}
        run: |
          mkdir -p secrets
          {
            printf 'INTEGRATION_TEST_EMAIL=%s\n' "$TEST_EMAIL"
            printf 'INTEGRATION_TEST_PASSWORD=%s\n' "$TEST_PASSWORD"
          } > secrets/integration_test.env
          if [ -z "$TEST_EMAIL" ]; then
            echo "::warning::INTEGRATION_TEST_EMAIL is unset — every Patrol flow will self-skip instead of testing anything."
          fi

      - name: Flutter deps + codegen
        run: |
          flutter pub get
          dart run build_runner build --delete-conflicting-outputs

      - name: Install patrol_cli (pinned — unpinned installs an incompatible CLI)
        # MUST pair with the `patrol` package in pubspec.lock: patrol 4.10.0
        # pairs with patrol_cli 4.8.0 (PATROL_CLI_VERSION above). A mismatched
        # pair fails at link time with undefined XCTest symbols in PatrolImpl.o,
        # or prints "Version incompatibility detected!" and refuses to run.
        run: dart pub global activate patrol_cli "$PATROL_CLI_VERSION"

      - name: Boot iOS simulator
        run: |
          set -euo pipefail
          xcrun simctl boot "$PATROL_DEVICE" 2>/dev/null || true
          xcrun simctl bootstatus "$PATROL_DEVICE" || true
          xcrun simctl list devices booted

      # Every *_test.dart in integration_test/ and integration_test/flows/,
      # minus integration_test/runner_exclusions.json, from
      # scripts/patrol-targets.mjs. Excluded today (see the file for each why):
      # the flows that spend on AI (ai_coach_chat, meal_plan_build), the ones
      # that need a clean install with no session (onboarding_signup,
      # account_delete, integrations_connect) and the interactive Google OAuth
      # flow (google_login).
      #
      # --bundle-id: pubspec's patrol.ios.bundle_id is the prod id; the dev
      # flavor installs com.milkman.mealvanaendurance.dev, and without the flag
      # every native query ($.native / $.platform) fails with "Application
      # com.milkman.mealvanaendurance is not running".
      #
      # PATROL_FAIL_ON_SKIP=true turns every flow's skipFlow() into a failure.
      # Patrol reports a markTestSkipped test as PASSED (Skipped: stays 0), so
      # without it the skip check below cannot see a flow that tested nothing.
      # Run those by hand against a freshly-erased simulator; the AI ones count
      # against a testing wave's cost caps.
      #
      # One invocation, many --target flags: patrol builds the app ONCE and runs
      # every flow against it. Looping `patrol test` per file would rebuild the
      # Xcode host each time — minutes of wall-clock and gigabytes of disk per
      # flow on this machine.
      #
      # Fail fast when the credentials are missing, INSTEAD of letting every
      # flow self-skip.
      #
      # Each flow calls markTestSkipped(noAuthSkipMessage()) when it cannot
      # authenticate, and `patrol test` exits 0 when tests skip. So an empty or
      # missing secrets/integration_test.env means every flow skips, patrol
      # exits 0, and this job reports GREEN having tested nothing at all. That
      # is the most dangerous failure mode the suite has: it looks like a pass.
      - name: Verify integration credentials are present
        run: |
          set -euo pipefail
          f=secrets/integration_test.env
          test -s "$f" || { echo "::error::$f is missing or empty — every Patrol flow would self-skip and this job would falsely report success. Set INTEGRATION_TEST_EMAIL/PASSWORD in repo secrets."; exit 1; }
          for k in INTEGRATION_TEST_EMAIL INTEGRATION_TEST_PASSWORD; do
            v=$(grep -E "^$k=" "$f" | cut -d= -f2- || true)
            test -n "$v" || { echo "::error::$k is empty in $f — Patrol flows would self-skip and this job would falsely report success."; exit 1; }
          done
          echo "integration credentials present"

      - name: Patrol flows (iOS)
        run: |
          set -euo pipefail
          export PATH="$HOME/.pub-cache/bin:$PATH"
          mkdir -p test-results
          # A broken exclusion list (a named flow that no longer exists) exits 1 here.
          list=$(node scripts/patrol-targets.mjs targets)
          test -n "$list" || { echo "::error::scripts/patrol-targets.mjs found no targets"; exit 1; }
          node scripts/patrol-targets.mjs count > test-results/expected-count
          echo "Patrol targets ($(cat test-results/expected-count) cases):"; echo "$list"
          echo "Excluded:"; node scripts/patrol-targets.mjs excluded
          targets=()
          while IFS= read -r t; do targets+=(--target "$t"); done <<< "$list"
          patrol test \
            "${targets[@]}" \
            --flavor dev \
            --bundle-id com.milkman.mealvanaendurance.dev \
            --dart-define-from-file=secrets/integration_test.env \
            --dart-define=PATROL_FAIL_ON_SKIP=true \
            -d "$PATROL_DEVICE" \
            2>&1 | tee test-results/patrol.log

      # A green `patrol test` is not the same as "the suite ran".
      #
      # Skips are invisible in the exit code, so a flow that quietly stops
      # testing — an empty catalog, a missing seed row, a self-skip someone
      # added to get CI green — leaves this job passing while covering less
      # than it claims. Assert the shape of the run, not just its exit status:
      # every target must have executed, and none may have skipped.
      - name: Assert the suite actually ran
        if: always()
        run: |
          set -euo pipefail
          log=test-results/patrol.log
          test -s "$log" || { echo "::error::no patrol output captured"; exit 1; }
          # Derived from the target list, so it follows the flows folder.
          EXPECTED_PATROL_TESTS=$(cat test-results/expected-count 2>/dev/null || node scripts/patrol-targets.mjs count)

          total=$(grep -oE 'Total: [0-9]+'      "$log" | tail -1 | grep -oE '[0-9]+' || echo 0)
          skipped=$(grep -oE 'Skipped: [0-9]+'  "$log" | tail -1 | grep -oE '[0-9]+' || echo 0)
          passed=$(grep -oE 'Successful: [0-9]+' "$log" | tail -1 | grep -oE '[0-9]+' || echo 0)
          echo "total=$total passed=$passed skipped=$skipped (expected total >= $EXPECTED_PATROL_TESTS)"

          if [ "$total" -lt "$EXPECTED_PATROL_TESTS" ]; then
            echo "::error::only $total Patrol tests ran, expected at least $EXPECTED_PATROL_TESTS. A target was dropped or the bundle failed to build."
            exit 1
          fi
          if [ "$skipped" -gt 0 ]; then
            echo "::error::$skipped Patrol test(s) SKIPPED. A skip is not a pass — it means that flow verified nothing. Fix the precondition (seed data, credentials) rather than leaving it skipped."
            grep -E '⏩|skipped' "$log" | head -20 || true
            exit 1
          fi
          echo "suite shape OK: $passed/$total passed, 0 skipped"

      - name: Shut down simulator
        if: always()
        run: xcrun simctl shutdown "$PATROL_DEVICE" || true
