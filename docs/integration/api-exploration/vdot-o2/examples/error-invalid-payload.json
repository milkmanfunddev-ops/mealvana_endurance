{
  "_comment": "OBSERVED against the live token endpoint https://app.vdoto2.com/oauth/token (HTTP 400). Cause: the form body's client_id or client_secret was empty, OR the body was sent as JSON instead of application/x-www-form-urlencoded. NOT a problem with the authorization code. Note this is NOT the RFC 6749 error shape.",
  "status": "NOK",
  "error": "invalid_payload"
}
