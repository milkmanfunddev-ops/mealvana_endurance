# Daily batch — 2026-07-21 (Formula engine rebuild + carried-over fixes)

Follow-through on the 2026-07-20 findings. Lee's directive: do both tracks (scoped
fixes + structural refactor), the additions (invariant test, generation ledger), all
five open steps from yesterday's report, and any cleanup possible. Everything landed
on the integration branch `claude/daily-2026-07-20` (local only, **not pushed**,
`develop` untouched, **no Notion writes**, **edge function NOT deployed**).

---

## TL;DR

- **The Critical formula bug (3a3e3fdb) is fixed at both levels.** Track 1's four
  scoped fixes are in, and Track 2 restructured `generateDuringPhase` so the
  guarantee is architectural: one shared food pool, personal-formula → template →
  rule cascade, and a closing pass (gut-cap-clamped carb gap-fill + shortfall
  computation) on **every** path. A during phase can now ship under-target only
  with a populated `shortfalls` field. Net server diff: **+432 / −1008 lines.**
- **The invariant is machine-checked.** `during-invariant-matrix.test.ts` runs the
  real cascade (fake supabase client) across activity × duration × gut level
  (including `null` and the legacy invalid `'medium'`) and asserts carbs ≥ 90 % of
  target OR a reported carbs shortfall. Mutation-verified: reintroducing the old
  guard fails the guard-regression suite. Full local algorithm suite 31/31.
- **The plan-generation ledger exists** (`plan_generation_log`, migration
  `20260721090000`, applied to DEV, recorded in the migration ledger). One row per
  plan: targets vs delivered, cascade path, shortfalls, pin decisions. This is
  Xuan's "measure the failure rate" ask from the bug card.
- **Both rejected branches from yesterday were rebuilt properly and merged**, plus
  the client half of the formula fix, plus the UX/tooling cleanups.

## What changed — server (commit `6ac71a55`)

- `during-phase.ts`: three-stage rewrite (see commit message for full detail).
  Gut level normalized via shared `normalizeGutTrainingLevel` (also replaces
  brick-handler's inline coercion); template tier guarded only by duration.
- Gap-fill: new `_shared/nutrition/during-gap-fill.ts` — append-only, clamps to
  gut per-hour caps (Lee's decision (a)), max 3 new items, never fills from
  electrolyte/hydration incidental carbs.
- Deleted: during LP tier, `postProcessDuringPhase` (deprecated), 
  `by-hour-apportionment.ts` (`by_hour_data` is now always `null`; the client
  already builds its own buckets). `lp-phase.ts` is after-phase-only.
- Shortfalls now computed at the single exit for template/rule/empty paths and the
  personal-formula path (previously template-only; rule/LP dropped them).
- `plan-generation-log.ts` + migration; best-effort insert via
  `EdgeRuntime.waitUntil`.
- `run-algorithm-tests.sh`: repaired the mangled `1w` entry —
  **search-nutrition-products tests had been silently skipped**; added `1j2`
  (invariant matrix).
- Wire contract preserved: response keys are copied explicitly in `index.ts`;
  `generation_path` is ledger-internal and never serialized to clients.

## What changed — client + carried-over fixes (merged branches)

1. **`claude/fix-3a3e3fdb-client-gut-level-shortfalls`** — always send
   `gut_training_level` (`?? 'moderate'`), fix the three invalid `'medium'`
   literals in `macro_targets_controller.dart`, parse `plan.during.shortfalls` in
   the mapper, render `MacroShortfallCard` for the During phase (mirroring Before).
2. **`claude/fix-3a3e3fdb-fuel-log-added-bucket`** (bug #3, option (a)) — real root
   cause was deeper than the rejected branch: ADD FOOD wrote to the **plan**
   sections (triple-adding across sub-phases) while the screen renders
   `fuelLogData`, whose write method had zero callers; and the renderer dropped
   null-sub-phase items. Fixed both halves; "Added" bucket renders after sub-phase
   groups; nothing invents a sub-phase. Mutation-tested both hunks.
3. **`claude/fix-3a3e3fdb-timeline-eaten-at-v2`** (bug #4) — cherry-picked the good
   core (302c6820 + 241be72e), added the missed `recipe_picker_screen.dart:234`
   `eatenAt`, audited every log call site (table in the agent report), new widget
   test drives the real screen. Bad test half confirmed absent.
4. **`claude/chore-ux-tooling-cleanups`** — dead "Notify Me" CTA now uses the
   established disabled pattern; Garmin banner dismiss exposed to VoiceOver
   (`Semantics` button, tap still dismisses); `sim-dev-login.sh` prints a real
   error instead of dying silently on a missing Keychain item.
5. **Docs** — `claude/docs-during-phase-refactor`: business-logic algorithm docs +
   code map updated to the new cascade; dated addendum on the architecture audit.

## Verification

- `flutter analyze`: 0 errors (104 pre-existing warnings, untouched baseline).
- Deno: full local algorithm suite **31/31**; invariant matrix 50 steps green;
  mutation check red-then-green documented above.
- Flutter: merged-tree runs — meal_logging + nutrition_plan + education +
  integrations + fuel_timeline + smoke suites all green (913 + 390 test runs).
- E2E (`--e2e`) not run — requires a deployed function; run after Lee deploys.

## Known follow-ups (not blocking, surfaced by the fuel-log rebuild)

- **Added fuel-log foods contribute zero macros**: `FuelLogItem.actualNutritionalInfo`
  returns null when `plannedQuantity <= 0` and added items write 0 — carbs/hr card
  and coach reports undercount. Pre-existing; now user-visible.
- `updateFuelLogItemQuantity` matches on `foodId + sectionId` only — same food in a
  sub-phase group AND the Added bucket updates both rows.
- Plan-mode add on a sub-phase section without a `:subPhase` target still appends
  to every sub-phase (other callers than fuel-log).
- During-phase pool no longer includes imported user foods (deliberate
  simplification; template solver never used them).
- Ledger covers single-activity plans; brick plans not yet logged.
- Prod still needs: `garmin-push` + `_shared/garmin/` deploy (held from 07-20) and,
  now, `generate-nutrition-plan-v3` + the `plan_generation_log` migration at the
  next prod cut.

## Session 2 additions (same day, after Lee's robustness directive)

- **Fuel-log macro bugs fixed & merged** (`a00dccba`): added foods now carry a
  `nutritionReferenceQuantity` so `actualNutritionalInfo` scales correctly
  (carbs/hr + coach reports count them); quantity taps match on
  `foodId+sectionId+subPhaseType+isAdded` so a planned row and an Added row of
  the same food no longer move together. Both mutation-tested.
- **Real-catalog invariant suite** (`during-invariant-real-catalog.test.ts`,
  runner 1j3): full cascade vs a snapshot of the real DEV catalog (25 templates
  / 92 foods; refresh instructions in the file header). 491 steps: free matrix ×
  4 body-size scales, every template pinned in its declared scope (176 renders,
  0 downgrades), personal formulas, diets/allergies. Three-macro invariant
  (carbs+sodium+fluid). The fake supabase client now implements eq/ov/or/in so
  fixture pools match production scoping — an earlier unscoped version
  "found" overshoots that were fixture artifacts.
- **Remote failure-mode E2E** (`during-invariant-remote-e2e.test.ts`, runner
  2b2): 13 payloads against the deployed fn incl. the exact 3a3e3fdb triggers.
- **DEV deployed** (Lee's robustness directive superseded the hold): function
  deployed twice (refactor, then brick shortfall wire). Ledger is receiving rows.
- **E2E suites brought to the real contracts**: honest-shortfall acceptance,
  <15g single-serving carb exception, After demoted to `[AFTER-TRIGGER-DESIGN]`
  warnings (After is deliberately trigger-not-dose — product decision pending),
  `during_segment_shortfalls` added to the brick wire (additive). Two silent
  TEST bugs fixed: diet tests sent `diet_type` (dropped — vegan was never
  requested!), and keyword matchers flagged "plant milk" as dairy/animal.
- **Old-vs-new A/B** (worktree at e867b8e2, same fixtures): old code delivered
  sodium up to 199-270% of target and carbs as low as 54% silently on
  small-target workouts; new code stays in band or reports. No regressions.
- **Verified vs live DEV**: index E2E 149 steps green · strict-macro 45/47 ·
  remote invariant 13/13 · local algorithm suite 32/32 · Flutter
  nutrition_plan+coach_mode 508 green post-merge.

### NEW findings for the board (pre-existing, kept red / to file)
1. **Before-phase water 0%** — Average Female Half profile: Algorithm C emits
   zero fluids against a real water target (strict-macro red #1).
2. **Before-phase sodium 154%** — Very Heavy Marathon profile overshoots even
   the +40%-tolerance band (strict-macro red #2).
3. **After-phase dosing tension** — trigger-not-dose design vs macros-v4 still
   emitting post_run dose targets the plan ignores (water 2-30% delivered).
   Product decision needed; all After band checks currently warn-only.
4. Fuel log: added rows can never be deleted (floor at qty 0); legacy added
   rows from the pre-fix window read 0 macros (unreleased branch only).

## Session 3 additions (prod update + before-phase tier fix)

- **PROD updated, backwards compatible, no v4 needed.** All wire changes are
  additive (old clients ignore `shortfalls`/`during_segment_shortfalls`;
  `by_hour_data` was already null on the dominant paths). Applied the
  `plan_generation_log` migration to prod (MCP apply_migration), deployed
  `generate-macros-v4` + `generate-nutrition-plan-v3` + **the held
  `garmin-push`** to prod. Verified live: strict-macro **47/47 vs PROD**,
  remote invariant 13/13 vs PROD, ledger receiving rows, 2h-before returns
  full meal. Prod schema parity confirmed first (personal_formulas ✓, all 6
  constraint columns ✓, catalog counts identical to dev).
- **Meal-tier bug fixed** (`38af2551`): spec (Notion 31fe3fdb) says ≥90 min →
  Meal+Snack+Top-off, 30–89 → Snack+Top-off, <30 → Top-off. Code had drifted
  to 2.5h/1.0h in three places (macros-v4 targets + sub-phases, shared
  pre-workout-targets.ts + timing label). All aligned; boundary tests added.
- **"Adjust macros → 0g before carbs" diagnosed server-side**: prod macros-v4
  is healthy (probed all normal payloads); only `is_fasted: true` (by design)
  or a rare 400 (1 in 24h in logs) produce zero. The failing e2e that looked
  related calls the RETIRED `generate-macros` name (stale test, known). Client
  investigation delegated (agent, branch `claude/fix-adjust-macros-zero-before`).
- **DEPLOY TRAP documented in deploy-edge skill**: plan-v3 bundles
  macros-v4's pre-workout.ts via cross-function import — Algorithm C changes
  need BOTH functions deployed.
- **Known drift flagged, not changed** (needs Lee's call): BUDGET_SPLITS carbs
  are 60/25/15 vs the spec's 60/30/10, and the 2-tier split normalizes to
  62.5/37.5 vs the spec's 75/25. Also macros-v4 still hard-neutralizes
  liked/disliked foods (the 07-03 rip-out was re-enabled elsewhere on 07-08
  but never here).

## Session 5 additions (decisions executed + brick parity + is_fasted)

- **Carb splits → spec 60/30/10** (two-tier renorm = exactly the spec's 75/25)
  and **before-phase food preferences re-enabled** in macros-v4 (all-disliked
  degrades honestly: DEV probe with a 21-food disliked list produced 74g/140g
  WITH shortfalls, not a silent empty). Both live on DEV + PROD, strict 47/47
  on both.
- **Brick plans are first-class now** (`7a9184b4`): pins + personal formulas
  plumbed through every brick phase (were silently ignored); brick After uses
  the recovery-template trigger design (was an LP dose path); brick plans
  write the ledger. Additive wire keys `after_metadata` / `after_shortfalls` /
  `after_pin_decision` on the brick plan. Deployed DEV + PROD, verified.
- **`is_fasted` persisted end-to-end** (`fda20736`): Supabase column on
  dev+prod (migration `20260721210000`), Drift folded into the unreleased v14
  (no bump), all mapper/sync points, creation-time persistence, and
  `regeneratePlan()` now reads the real flag (was hardcoded false). 721 tests
  green on the merged tree.
- **Flowchart** (`docs/business_logic/nutrition-plan-v3-flowchart.svg/png`)
  updated + re-rendered to match the fixed brick branch and ledger coverage.
- Remaining scheduled item: **After-phase rehydration-only fill** (keep
  trigger design for food; dose fluid+sodium with capped sippable volume) —
  designed, not built.
- Out-of-scope finds: transitions still template-0→LP→greedy (fine — tiny
  fixed targets); brick regenerate silently no-ops on empty brickSegments
  (pre-existing).

## Open actions

| # | Action | Owner |
|---|--------|-------|
| 1 | Review integration branch; merge to `develop` when satisfied | Lee |
| 2 | Deploy `generate-nutrition-plan-v3` to DEV (`/deploy-edge`); table already exists | Lee |
| 3 | Run `--e2e` suite against DEV after deploy | Lee/me |
| 4 | At next prod cut: prod migration + fn deploy (+ held garmin-push) | Lee |
| 5 | Notion: link bugs to the cut card when cutting (Tasks relation only) | me (post-approval) |
| 6 | Decide the 4 Formula Kit "Needs Decision" items (carb-ratio one is now safety-netted by gap-fill but still worth deciding) | Lee |
| 7 | File follow-up bugs: added-foods-zero-macros, quantity-update collision | me (post-approval) |
