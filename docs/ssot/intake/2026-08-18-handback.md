# APP-SIDE HANDBACK — 2026-08-18 mark-done ruling (Q-D7 / Q-018) → `daily-macros-dashboard@v3`

For the app-repo coding agent. Execute top to bottom. Resolve `$APP_ROOT` via
`mealvana_endurance/workspace.env` / `find_workspace` — never hardcode paths. Everything cited below
is folded in the qa commit this file arrived in (`git log -1 --format=%H -- intake/2026-08-18-handback.md`
on `qa/daily-macros`).

**Read this first — the version story.** The ruling is a CONTRACT CHANGE by the register's own test
(a ratified vector expected, a manifest assertion and a worked consequence all change), so it is
**`@v3`**, not a `@v2.1` point tag; `@v2` stays immutable and still says `= now`. Xuan ratified the
written v3 text 2026-08-18 and **`daily-macros-dashboard@v3` is tagged** — pin the TAG. `land-bundle`
lands `@v3`. **`@v2` will not land as-is:** the app's tests already pin the ruled behaviour, which is
RED against `@v2`'s `g1` and `two-time-mark-done` by design — exactly the `@v1 → @v2` precedent (no
throwaway point tag). Full v3 handoff (delta + conflict watchlist): `bundles/daily-macros-dashboard.handoff.md`.

## 1 · Re-sync the SSOT mirror

- [ ] Re-sync `$APP_ROOT/docs/ssot` as a **verbatim mirror** of this qa commit (specs + vectors +
      conformance manifests + `bundles/`), and update `$APP_ROOT/docs/ssot/SSOT_SOURCE.txt`'s commit
      pin to the tag `daily-macros-dashboard@v3` (mirror the tag's content, not the branch head).

## 2 · Flip the test-only twin, then re-run conformance and report the green count

- [ ] `supabase/functions/_shared/workouts/workout-times.ts` `applyMarkDone` — the deliberately-held
      `= now` twin now flips to **`= planned_time`** (the vector regeneration it was waiting for has
      landed). Commit message cites Q-D7 / `platform-resolution.md` two-time model v2.
- [ ] Deno vectors runner over `vectors/daily-macros/*.json` — **expect your @v2 green count + 2,
      all green** (you reported 168/168 at @v2; the raw vector count is 170 → 172 — the "168" in the
      v1/v2 manifests was a stale figure, see OPEN-QUESTIONS Q-018 — so report whatever your runner
      counts, and it must be +2 and fully green). `platform-resolution.json`: `two-time-mark-done`
      regenerated → actual 1050 / planned 1050 / display 1050 for planned 1050 & now 900; NEW
      `two-time-mark-done-after-planned` (now 1200 → still 1050); NEW
      `two-time-sync-overwrites-mark-done` (gesture `sync`, measuredStartMin 1042 over a MANUAL 1050 →
      actual 1042, source GARMIN, display 1042 — harness co-evolution: the `two-time` branch of
      `vectors.conformance.test.ts` grows a `sync` gesture arm beside `mark_done`/`mark_undone`; that
      is implementation work, not a spec gap).
- [ ] Dart suites: intraday-display consumer, `daily_baseline_calculator_test.dart` +
      `parity-fixtures.test.ts` (parity twins), design goldens + gesture tests. Report counts.

## 3 · Implement what the ruling gates

The intake says the production paths are already in (`ActivitiesRepository.markWorkoutDone` writes
`actual_time = planned_time ?? scheduled_date_time`; `WorkoutCardData.markDoneAllowed` false on a
future day; gestures / repository / screen / Patrol tests pin it). Remaining:

- [ ] Reconcile the app's gesture tests against **manifest v3** (18 tests): `g1_swipe_right_marks_done`
      now asserts `= planned_time` with `now != planned_time` on BOTH sides (before and after) and on
      the current AND a past day, card keeps its slot, nothing lands on today; NEW
      `g1_future_day_not_offered` (right-swipe on a future-day PLANNED/SKIPPED → zero translation, no
      reveal, no write, no state change; left-swipe still reveals per G4; G2 still runs on a legacy
      confirmed future card); `skipped_swipe_right_recovers` asserts `= planned_time` and recovery on
      the card's own day. Name the app test files against these ids (the audit maps by id).
- [ ] `sync_upgrades_confirmed_to_verified` — unchanged; confirm the seam overwrites a mark-done
      `actual_time` (now `= planned_time`) with the measured start, MANUAL → GARMIN.
- [ ] `WorkoutCardData.markDoneAllowed` semantics = spec G1 clause exactly: false only when the card's
      DAY is in the future (not when `planned_time` is later today — a same-day workout is markable
      before its planned time; `workout_so_far` counts it in full via `actual_time` presence).
- [ ] Nothing to change in the display consumer: `workout_so_far` keys off `actual_time` presence
      (`intraday-display.md` §1) — unchanged by Q-D7.

## 4 · Flip test-plan rows

- [ ] `docs/feature-test-plans/macro-dashboard.md` (mirror): flip I2 and the lifecycle-chain
      "gesture → state + write" row (⬜ → ✅ with the test file paths) — a described test is not a
      pinned test; flip only what actually runs in CI.

## 5 · Cross-updates

- [ ] The app README ledger entry that records "implemented ahead" for this item: point it at Q-D7 /
      Q-018 and this qa commit; note that the fold classified it `@v3` (contract change), so the
      ledger's "@v2 implementation" line should read "@v3 behaviour, RED against @v2 by design".
- [ ] Two sibling ruling-requests from the same implementation pass are still open in qa
      (`intake/2026-08-18-skipped-row-sync-match-window.md`,
      `intake/2026-08-18-platform-declared-skip-semantics.md`) — untouched by this fold; they enter
      via intake-triage.
