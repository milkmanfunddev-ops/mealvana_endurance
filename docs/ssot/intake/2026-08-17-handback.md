# APP-SIDE HANDBACK — 2026-08-17 intake batch (all six items resolved)

For the app-repo coding agent. Execute top to bottom. Resolve `$APP_ROOT` via
`mealvana_endurance/workspace.env` / `find_workspace` — never hardcode paths. Every ruling cited
below is folded in this qa commit (`git log -1 --format=%H` on `qa/daily-macros` at this file's
introduction — the same commit this file arrived in; a point tag `daily-macros-dashboard@v1.1` is
recommended and, once cut, is the thing to pin).

## 1 · Re-sync the SSOT mirror

- [ ] Re-sync `$APP_ROOT/docs/ssot` as a **verbatim mirror** of this qa commit (specs + vectors +
      conformance manifests), and update `$APP_ROOT/docs/ssot/SSOT_SOURCE.txt`'s commit pin to it.

## 2 · Re-run conformance and report the green count

- [ ] Deno vectors runner over `vectors/daily-macros/*.json` — **was 167/168; expect 168/168**
      (`override-ceiling-branch` is regenerated: inputs now session 2770, expects carb 900 /
      prot 115 / fat 70 / adjusted true / carbAtCeiling true / post-override EA 30.0).
- [ ] Dart suites: intraday-display consumer, `daily_baseline_calculator_test.dart` +
      `parity-fixtures.test.ts` (parity twins), design goldens + gesture tests. Report counts.

## 3 · Implement what the rulings gate

- [ ] **Q-016 (manual profile edits → today + future recalc).** The one-line fix in
      `nutrition_profile_screen.dart`: on any MANUAL write to an engine input (weight, height,
      BF%, lifestyle, weekly hours, carb-cycle opt-in, phase), invalidate **today and all future
      cached days** (never past days) and refresh the daily-macros controller. Plus one
      behavioral chain test: edit weight in Settings → today's dashboard targets change, past
      days unchanged. Closes the Major bug
      `ops/data/bug-reports/2026-08-17-profile-save-macro-cache-stale.md` (cross-update its
      status when landed).
- [ ] **Q-017 (carb cycling no-op).** No engine change — the v1 contract stands and you must NOT
      compensate. Only check: if any opt-in UI copy promises visible train-low numbers today,
      soften it to setting-recorded language. The real fix (10b exemption) arrives with the next
      bundle version.
- [ ] **Q-D4 (no awaiting-sync glyph).** Nothing to build — shipped behavior (glyph-less card,
      sync upgrade to verified) is now the ratified contract. Do not add a glyph; the blessed
      glyph-less golden stands.
- [ ] **Q-D5 (SKIPPED state).** Replace the provisional prompt: past-day unresolved workout
      renders planned treatment + a `Skipped` chip; **remove** the "Did this happen? Swipe right
      to mark done" copy and the same-day-22:00 trigger (current day never shows SKIPPED — it
      stays PLANNED). SKIPPED fuel/kcal excluded from all day figures; right-swipe recovers to
      DONE_CONFIRMED (fuel re-enters). Regenerate `workout_card_skipped.png` **with a commit
      message citing the Q-D5 spec change** (the goldens-regeneration rule). Implement the three
      new gesture tests in `conformance/design/macro-dashboard.gestures.yaml`:
      `skipped_only_on_past_days` (incl. the negative half), `skipped_fuel_not_counted`,
      `skipped_swipe_right_recovers`.
- [ ] **README erratum (Dart mirror).** Nothing to build; the release-coupling rule is now
      ratified — preview (`DailyBaselineCalculator`) and deployed engine must ship the same
      formula generation. Treat it as a release checklist item.

## 4 · Flip test-plan rows

- [ ] `docs/feature-test-plans/macro-dashboard.md`: flip the rows the above tests pin
      (⬜ → ✅ with the test file path) — a described test is not a pinned test; flip only what
      actually runs in CI.

## 5 · Ops-side cross-updates

- [ ] `ops/data/bug-reports/2026-08-17-profile-save-macro-cache-stale.md` — record that the fix
      scope is ruled (Q-016: today + future) and link the landing commit when done.
