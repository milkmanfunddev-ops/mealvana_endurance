> **RESOLVED 2026-08-17 → spec/daily-macros/README.md Engine paragraph corrected (mirror + parity contract) + release-coupling sentence RULED as post-ratification addition**

type: spec-erratum
bundle: daily-macros-dashboard@v1

## Why this matters
The README's "no Dart mirror" claim hides an entire second engine surface from conformance audits — the client-side preview math can drift from the ratified formulas with no document saying it must not.

## Artifact + location
`spec/daily-macros/README.md`, the **Engine** paragraph:

> "**Engine:** B — daily macros. **Server-only** (`calculate-daily-macros` edge function); there is no Dart mirror, so vectors will run against the edge function, not `OfflineMacroCalculator`."

## Why it is wrong
True when recorded (2026-07-28); stale since the app's onboarding-redesign branch added `DailyBaselineCalculator` (`app/lib/features/nutrition_plan/application/daily_baseline_calculator.dart`) — a client-side Dart mirror used by the onboarding plan preview (offline, account-less). As of the app branch `feature/daily-macro-dashboard-redesign` it mirrors: F1 (RMR), F2 (baseline), the step-9 clamps, F3 (zone→IF), F4 (session cost), F5 (carb demand incl. Q-003 strength flat rate), F15 (iterative TEF, uncapped residual), and assembly step 10b (Q-014 fat cap, applied in `PlanPreviewService`).

## Smallest correction
Replace "there is no Dart mirror…" with a paragraph recording:
- the mirror and its formula coverage (above);
- its conformance contract: the shared parity fixture `app/test/features/onboarding/fixtures/plan_preview_parity.json`, generated by `app/supabase/functions/calculate-daily-macros/tools/generate-parity-fixtures.ts` from the vector-pinned TS formulas (never hand-edited), consumed by both `parity-fixtures.test.ts` (Deno) and `daily_baseline_calculator_test.dart` (Dart);
- the release-coupling consequence: preview and deployed engine must ship the same formula generation, or new users see a preview disagreeing with their post-onboarding plan.
