type: ruling-request
bundle: carb-loading follow-up (post-release-1)

# PARKED: catalog unification to the S9 one-store endstate

Ruled endstate (S9, 2026-09-21, Lee+Xuan): shared library + `is_carb_loading` + per-slot
suitability; separate carb tables retired. Release-1 ships the ruled stopgap instead (D-022,
G21 fork option B, Xuan 2026-09-25). This file is the resume pointer — collectors resurface it.
Scope when picked up: library columns, `meal_types` carry-over migration (Lee's domain), slot
page repoint, old-store retirement, red = G21 seam L2 on the unified source + zero readers of
`carb_loading_foods`.
