# Sim run — Ravi — 2026-08-02 (qa-smoke skill audit run)

**Athlete:** Ravi (`ath12`, `ravi@test.com`) — `allergies: [dairy, gluten]`, omnivore, 161 lb /
73.03 kg, primary sport run. Profile: `qa/profiles/athletes.json`.
**Login:** none needed — sim was already signed in as `ravi@test.com` (verified in Settings AND by
the identity signature below). Claude never entered a password.
**Env:** iPhone 17 sim (`860F418A-22C9-42A7-8920-CB04135D2857`), dev flavor
(`com.milkman.mealvanaendurance.dev`), dev Supabase. **Build: freshly rebuilt at HEAD `3e3dc86f`
(2026-07-31)** via `app/scripts/run_dev.sh` — the previously installed bundle was a stale
**integration-test runner** (`flows.recommendation_stacking_flow_test` harness screen), not the app.
**Identity check:** pre-carbs = round(73.03 × clamp(1.5 h, 0.5, 4)) = **110 g** — matched on-screen
PRE carbs exactly → confirmed Ravi, not a stale/mislabeled session.
**Scope:** formula usage during recommendation — `qa/spec/recommendation/generate-plan.md`
(H2/H3/H4/H5/H6/H7 + D1/D2), with the fueling macro summary as the engine ⇄ screen cross-check.
**Driving layer:** mobile MCP unavailable this session — drove via `idb ui tap/swipe` +
`xcrun simctl io screenshot`. Worked reliably; coordinates from screenshots (points = px/3).

## Workouts driven

| # | Sport / inputs | Window | Key output | Invariant(s) exercised |
|---|---|---|---|---|
| 1 | 12 mi Run · 1h48m (108 min) · 9:00/mi · fasted OFF · 68 °F (20 °C) · 60 % humidity · **pre-existing Oatmeal pin from 2026-07-28 run still on account** | 1h30m (= exactly 90 min) | Macro summary PRE 110g/18g/8oz/150mg · DURING 95g/0g/44oz/1071mg. Plan detail: BEFORE = Full Meal (**2.5 cups Oatmeal**, 67g/17oz/0mg) + Snack (Rice Cake + Jam) + Top-Off (Water + Electrolyte Packet); BEFORE rollup 101g ∈ [96,124] · **21oz vs [7,10]** · 144mg ∈ [100,200]. DURING 100g ∈ [81,108] (55.6 g/hr) · 46oz ∈ [37,50] · 1160mg ∈ [857,1285]; foods Energy Gels + Water + Pickle Juice Shot. AFTER: Bananas, Plant Milk, Mixed Berries, Plant Protein. Pin banner "1 honored · 4 skipped": **Meal: Oatmeal ✓ green check, NO warning** | H3 ✅ · **H2/D1 ❌ (D-003 still reproduces at HEAD)** · H4 ✅ · H5 (=90 → meal+snack+top-off; boundary ambiguity → D-015) · H6 🟡 (fluids out of range → D-014) · H7 ✅ |

## Spec ⇄ screen comparison (macro summary, workout 1)

Expected computed from ratified specs (pre-workout slices per the **v1 code contract** — the v2/v5
spec rewrites in the working tree are PROPOSED, not ratified; code implements v1).

| Metric | Spec expected | Screen | Verdict |
|---|---|---|---|
| PRE carbs | 73.03 × 1.5 = 109.55 → 110 g | 110 g | ✅ (identity signature) |
| PRE fluids (v1 tier) | 250 ml = 8.45 oz | 8 oz | ✅ |
| PRE sodium (v1 tier) | 150 mg | 150 mg | ✅ |
| DURING carbs | band(108′)=45–60 × gut 1.0 → mid 52.5 g/hr × 1.8 h = 94.5 g | 95 g | ✅ |
| DURING fluids | 1.28×0.92×1.02 = 1.201 L/h → 60 % → 721 ml/h × 1.8 = 1298 ml = 43.9 oz | 44 oz | ✅ |
| DURING sodium | 0.721 × 825 = 595 mg/h × 1.8 = 1071 mg | 1071 mg | ✅ exact |

Sweat inputs used the app defaults (medium 1.28 / average 825) per the known seed caveat — for Ravi
these coincide with his profile, so the caveat did not bite.

## Invariant results (this run)

| Invariant | Verdict | Note |
|---|---|---|
| H1 — graceful degradation | ⬜ not tested | not forceable from the UI; needs harness |
| H2 — safety / informed override | ❌ **FAIL (recheck)** | pinned gluten Oatmeal served to gluten-allergic Ravi with green ✓ and **no warning** at banner, card, or expanded-row level — **D-003 unchanged at HEAD 3e3dc86f** |
| H3 — pin priority | ✅ pass | suitable Meal pin honored over safe fallback |
| H4 — fallback filtering | ✅ pass | every non-pinned food dairy/gluten-free (Rice Cake+Jam, Water+Electrolyte, Gels, Pickle Juice, Plant Milk, Plant Protein) |
| H5 — pre-workout stacking | ✅ pass (boundary noted) | window exactly 90 min → {meal, snack, top-off}; spec text puts 90 in BOTH the 30–90 and ≥90 bands → D-015 |
| H6 — in-range + gap-fill | 🟡 partial | carbs + sodium in range both phases; DURING gap-fill present (Water + Pickle Juice). **BEFORE fluids 21 oz > high 10 oz** — pinned Oatmeal carries 17 oz intrinsic fluid → D-014 (unconfirmed candidate) |
| H7 — gut cap | ✅ pass (unstressed) | 100 g / 1.8 h = 55.6 g/hr ≤ 70 |
| H8 — input validation | ⬜ not tested | form constrains input; needs harness |
| H9 — determinism | ⬜ not tested | single generate; needs harness |
| H10 — brick composition | ⬜ not tested | run-only this session |
| S1–S5 — soft | ⬜ not observable | harness only |
| D2 — suitable-pin scope | ✅ pass | Meal pin honored only for Meal; other 4 phases "No pin found" |

## Findings raised
- **D-003 recheck** — still reproduces on latest dev build (2026-08-02, HEAD `3e3dc86f`). Status
  remains `pending-ruling`; entry updated with recheck note.
- **D-014 (NEW, unconfirmed candidate)** — pinned formula's intrinsic fluid blows BEFORE fluids past
  the range high (21 oz vs [7,10]); H6 miss caused by honoring H3. See `qa/DEVIATIONS.md`.
- **D-015 (NEW, spec clarification)** — H5 band text overlaps at exactly 90 min; observed behavior
  resolves 90 → ≥90 (meal included). One-word spec fix ("30–<90") or ratify observed.

## Caveats hit / audit notes for the skill (see full audit in chat report)
- **Installed bundle was a stale integration-test build**; `sim-ensure.sh` printed "✓ simulator
  ready" anyway. Rebuilt with `app/scripts/run_dev.sh -d <udid>`. The skill needs a "verify the app
  UI actually renders (screenshot), not just launch-succeeded" step and a rebuild recovery path.
- **Mobile MCP absent** — idb + simctl screenshots are a full substitute; worth documenting in
  `sim-navigation.md` as the fallback driving layer.
- **Pre-existing pin changed the "plain" run**: seeded athletes do NOT reliably start pin-free once
  prior QA runs have touched them. Read the pin banner BEFORE interpreting H4/H6.
- Already-signed-in-as-target-athlete: skipped logout/login; identity check (2c) carried the proof.
- Coordinate practice that worked: screenshot px ÷ 3 → points for `idb ui tap`; occasion cards
  expand in place; settings gear at ~(371, 98) hit first try.
