--- lib/features/nutrition_plan/presentation/screens/activity_detail_screen.dart
+++ lib/features/nutrition_plan/presentation/screens/activity_detail_screen.dart
@@ -402,14 +402,6 @@
                             isBrick: isBrick,
                             carbAdjustment: carbAdjustment,
                           ),
-                  onEditFeedback: (rating, notes, carbAdjustment) =>
-                      _handleFeedbackEdit(
-                        context,
-                        state,
-                        rating,
-                        notes,
-                        carbAdjustment,
-                      ),
                   onRatingChanged: (rating) {
                     final controller = _getControllerNotifier();
                     controller.updateCompletionRating(rating);
@@ -904,40 +896,6 @@
     }
   }
 
-  Future<void> _handleFeedbackEdit(
-    BuildContext context,
-    ActivityDetailState state,
-    int rating,
-    String? notes,
-    CarbAdjustmentLevel? carbAdjustment,
-  ) async {
-    final controller = _getControllerNotifier();
-    final activity = state.activity;
-    if (activity == null) return;
-
-    if (carbAdjustment != null && !activity.isBrick) {
-      await controller.applyCarbFeedbackAdjustment(
-        carbAdjustment,
-        isEdit: true,
-      );
-    }
-
-    await controller.updateCompletionFeedback(
-      rating: rating,
-      notes: notes,
-      carbAdjustment: carbAdjustment,
-    );
-
-    controller.trackEvent('workout_feedback_edited', {
-      'activity_id': activity.id,
-      'rating': rating,
-      'has_notes': notes?.isNotEmpty ?? false,
-      if (carbAdjustment != null) 'carb_adjustment': carbAdjustment.name,
-    });
-
-    if (mounted) {
-      MealvanaSnackbar.showSuccess(context, 'Feedback updated');
-    }
-  }
-
   void _swapFood(
     BuildContext context,
     ActivityDetailState state,
