--- lib/features/nutrition_plan/presentation/widgets/activity_detail/activity_detail_action_buttons.dart
+++ lib/features/nutrition_plan/presentation/widgets/activity_detail/activity_detail_action_buttons.dart
@@ -160,28 +160,4 @@
         );
       },
     );
   }
-
-  void _handleEditFeedback(BuildContext context) {
-    final duringCarbRateGPerH =
-        ActivityDetailHelpers.computeDuringCarbRateGPerH(
-          activity: state.activity,
-          nutritionPlan: state.nutritionPlan,
-        );
-    final initialCarbAdjustment = CarbAdjustmentLevel.fromRatingValue(
-      state.activity?.nutritionRating,
-    );
-
-    showDialog(
-      context: context,
-      builder: (dialogContext) => PostWorkoutFeedbackDialog(
-        isEditMode: true,
-        initialRating: state.activity?.completionRating,
-        initialNotes: state.activity?.completionNotes,
-        initialCarbAdjustment: initialCarbAdjustment,
-        duringCarbRateGPerH: duringCarbRateGPerH,
-        onComplete: (rating, notes, carbAdjustment) {
-          Navigator.of(dialogContext).pop();
-          onEditFeedback(rating, notes, carbAdjustment);
-        },
-      ),
-    );
-  }
 }
