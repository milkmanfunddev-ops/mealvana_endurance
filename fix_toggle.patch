--- lib/features/nutrition_plan/presentation/providers/activity_detail_controller.dart
+++ lib/features/nutrition_plan/presentation/providers/activity_detail_controller.dart
@@ -2354,7 +2354,10 @@
       loadExistingFuelLog();
     }
 
-    state = AsyncData(currentState.copyWith(fuelLogViewMode: newMode));
+    final latestState = state.value;
+    if (latestState != null) {
+      state = AsyncData(latestState.copyWith(fuelLogViewMode: newMode));
+    }
   }
 
   /// Parse activity.fuelLogData into state for editing.
